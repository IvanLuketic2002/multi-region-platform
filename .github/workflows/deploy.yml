name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ github.sha }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Backend Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./backend
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/multi-region-backend:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/multi-region-backend:${{ github.sha }}

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    strategy:
      # Matrica omogućava da se isti koraci izvrše za više različitih vrednosti (IP adresa) paralelno!
      matrix:
        host: [ "${{ secrets.SSH_HOST }}", "${{ secrets.SSH_HOST_SECONDARY }}" ]
    steps:
    - name: Deploy to AWS EC2 via SSH
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ matrix.host }}
        username: ubuntu
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        script: |
          echo "Povezan na AWS server: ${{ matrix.host }}. Pokrećem deploy..."
          
          # Čekamo 15 sekundi da budemo sigurni da je Docker instaliran ako je server tek podignut
          sleep 15
          
          # Zaustavi i obriši stari kontejner ako postoji
          sudo docker stop backend-app || true
          sudo docker rm backend-app || true
          
          # Povuci najnoviju sliku sa DockerHub-a
          sudo docker pull ${{ secrets.DOCKERHUB_USERNAME }}/multi-region-backend:latest
          
          # Pokreni novi kontejner na portu 8000
          sudo docker run -d \
            --name backend-app \
            -p 8000:8000 \
            --restart always \
            ${{ secrets.DOCKERHUB_USERNAME }}/multi-region-backend:latest
          
          echo "Deploy na ${{ matrix.host }} je uspešno završen!"
